#include <iostream>

#define IOS ios::sync_with_stdio(0);cin.tie(0);cout.tie(0)
#define ll long long;
using namespace std;

void solve() {
    int y; cin >> y;
    cout << y << "\n";
}

int main() {
    IOS;
    int t; cin >> t;
    while(t--) {
        solve();
    }
}